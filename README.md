When looking at commits:  
Root is Janvi Gianchandani  
LIYA-JOE is Liya Joe  
  
Current files in the repository (modified by Janvi and Liya) are:  
initial_main.nf = the original file containing the incomplete pipeline to be fixed   
main.nf = fixed pipeline containing the fastqc, trimmomatic, indexing and alignment steps  
modular_main.nf = modularised pipeline with the 4 steps simplified (refer to the modules folder for more information)  
.gitignore = contains large files, such as the outputs generated by the pipeline, that should not be tracked by Git  
nextflow.config = contains execution settings/parameters for the pipeline  
& All the fasta files initially provided  

Some of the errors faced & fixed:  
1) The params.reads path was corrected to a more flexible wildcard pattern (*_{1,2}.fq.gz).  
2) The typo in params.outDir was fixed to params.outdir  
3) A new parameter params.reference was added for the alignment step  
4) New channels were added for the alignment step  
5) Two new processes were added: indexing and alignment  
6) Labels were added for each of the 4 processes (refer to the nextflow.config file for more details)  
7) Emit labels were added to the trimmomatic outputs to further access them in downstream processes, like the alignment  
8) The script in trimmomatic process was split across 2 lines, so we used line continuation (\) to maintain correct command formatting  
9) The bwa-mem2 tool was not being recognised, so we provided full paths in the indexing and alignment scripts  
10) Corrected the typo in the nextflow.config file, and then we were able to remove the full paths and run the pipeline again  



